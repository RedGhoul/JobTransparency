@model Jobtransparency.Models.Entity.JobGetter.PositionCities

@{
    ViewData["Title"] = "Details";
}
<section class="site-section" style="padding-top:200px; padding-right: 20px; padding-left: 20px;">
    <div class="container">
        <h1>Details</h1>

        <div>
            <h4>PositionCities</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Name)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.JobGettingConfig)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.JobGettingConfig.Id)
                </dd>
            </dl>
        </div>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
            <a asp-action="Index">Back to List</a>
        </div>
        </div>
</section>